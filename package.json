{
  "name": "assignment",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup": "yarn workspace frontend install && yarn workspace backend install",
    "frontend": "yarn workspace frontend start",
    "backend": "yarn workspace backend start"
  },
  "devDependencies": {}
}
